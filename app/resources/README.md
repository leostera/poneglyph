This directory is for external runtime resources bundled with Poneglyph.app.

The future Rust daemon binary should live under `resources/bin/` so it can be
packaged outside the ASAR archive and spawned safely by the Electron main
process.
