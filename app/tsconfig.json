{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "lib": ["DOM", "DOM.Iterable", "ESNext"],
    "types": ["bun", "node"],
    "jsx": "react-jsx"
  },
  "include": [
    "src",
    "electron",
    "scripts",
    "vite.config.ts"
  ]
}
